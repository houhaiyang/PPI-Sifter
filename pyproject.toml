[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ppisifter"
version = "0.2.0"
description = "PPI-Sifter rewritten to align with B-PPI backbone and residue-level interpretability"
requires-python = ">=3.10"
